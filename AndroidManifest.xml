<manifest package="com.example.smartparking"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>


    <application
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"


    android:name=".App"
        android:allowBackup="true"
        android:label="SmartParking"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- LoginActivity (Launcher) -->
        <activity
            android:name="com.example.smartparking.ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- User Dashboard -->
        <activity
            android:name="com.example.smartparking.ui.UserDashboardActivity"
            android:exported="false" />

        <!-- Parking Details Activity (USED BY ParkingAdapter.kt) -->
        <activity
            android:name="com.example.smartparking.ui.ParkingDetailsActivity"
            android:exported="false" />

        <!-- Booking -->
        <activity
            android:name="com.example.smartparking.ui.BookingActivity"
            android:exported="false" />

        <!-- Admin Dashboard -->
        <activity
            android:name="com.example.smartparking.ui.AdminDashboardActivity"
            android:exported="false" />

        <!-- Signup -->
        <activity
            android:name="com.example.smartparking.ui.SignupActivity"
            android:exported="false" />

        <activity
            android:name="com.example.smartparking.ui.admin.AdminBookingsActivity"
             />

    </application>

</manifest>
